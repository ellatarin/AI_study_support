ELEVENLABS_API_KEY=your_api_key_here

# OpenRouter API key, read by src/pipeline/openrouter.ts. Every LLM stage from
# Stage 3 (transcript structuring) onwards needs it; a copy of this file
# without it produces a pipeline that runs Stages 0-2 and then dies.
OPENROUTER_API_KEY=
