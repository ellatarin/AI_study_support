# Do not write a `packageManager` field into package.json on install.
# pnpm adds one automatically, pinning an exact pnpm version and integrity
# hash. This project deliberately does not pin its package manager, and the
# auto-added field otherwise reappears in the working tree after every install.
manage-package-manager-versions=false
